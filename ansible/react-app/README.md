#clone repo from git and run the application in the targeted servers

#steps

->install node,npm and git as root user


->clone the repo (define the path as well as folder_name )

->check up the permissions

-> insatll the required packages inside the destination

-> run the server 


#cmd used

nohup-> keeps process running even after logout,terminal close

